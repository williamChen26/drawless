# drawless Agent 工作说明

本文件给后续参与 drawless 的 agent 使用。当前阶段的核心任务是梳理项目逻辑和需求逻辑，不做 UI 优化。

## 当前优先级

1. 保持 tldraw 协作链路清晰。
2. 保持 shared 类型边界清晰。
3. 保持后端 sync 房间生命周期清晰。
4. 保持 Next 路由和客户端连接逻辑简单。
5. 暂缓所有视觉优化和产品化交互。

## 工作原则

- 不新增 AI 行为，除非用户明确要求开始接入 AI。
- 不新增复杂 UI 组件、动效、主题、品牌视觉。
- 不把业务契约散落在 web/server 包里；跨包结构优先放入 `packages/shared`。
- 修改 shared 类型时，每个属性都要写中文注释。
- 保持 tldraw document 是画布事实源。
- 不新增第二套画布图结构事实源。
- 不为了好看引入额外状态或交互逻辑。

## 目录约定

- `apps/web`：只放前端路由、tldraw 挂载、协同客户端、浏览器身份。
- `apps/server`：只放后端健康检查、就绪检查、WebSocket sync、房间注册表。
- `packages/shared`：放共享类型、Zod schema、跨端校验和后续 AI 扩展点。

## UI 约束

当前 UI 只作为逻辑验证壳层：

- 顶部栏只展示必要调试信息。
- 主区域只承载 tldraw。
- 不新增 landing page。
- 不新增营销文案。
- 不新增视觉主题。
- 不新增 hover、动画、装饰元素。

## AI 约束

当前 AI 功能不启用。后续接入 AI 时：

- 先设计 shared 契约。
- 再设计服务端或受控前端边界。
- 不允许 AI 直接绕过协同边界修改画布。
- 不允许把 AI 上下文和 tldraw 文档事实源混成两套互相竞争的数据。

## 验证命令

完成代码修改后默认运行：

```bash
pnpm check
```

如果只改文档，可以说明未运行测试。

## 注释要求

- 代码里的自然语言注释使用中文。
- shared 类型的每个属性必须有中文注释。
- 技术名词可以保留原名，例如 tldraw、WebSocket、Next.js、Fastify。
