# 贡献指南

先阅读 [README](./README.md)、[架构说明](./ARCHITECTURE.md) 和 [AGENTS.md](./AGENTS.md)。当前工作以已有协同链路的安全和正确性为主，功能扩展应先讨论需求和 shared 契约。

## 开发约定

1. 使用 Node.js 22.13+ 或 24.x、pnpm 9.15.0 和仓库锁文件。
2. 从独立分支提交小范围修改，说明触发场景、行为变化和验证结果。
3. 跨包业务契约放入 `packages/shared`；所有新增 shared 属性和代码自然语言注释使用中文。
4. tldraw document 是画布事实源，不能新增第二套可写图结构。AI 工具必须经过房间权限和协同链路。
5. 修复逻辑问题时添加能覆盖原始失败的测试。提交前运行 `pnpm check`；依赖变化还需运行 `pnpm audit:dependencies`。
6. 不提交 `.env`、模型 key、房间签名链接、运行时数据库、用户画布内容或个人绝对路径。

完整验证不需要模型 key，也不会创建真实 GitHub Issue。真实模型验收需使用维护者明确授权的测试账户与费用预算。

## 提交和发布

PR 说明应包含问题、最终行为、测试和已知限制。接口、部署变量和存储行为变化必须同步文档。CI 要求检查和依赖审计通过。

当前所有应用包都是 private，尚不向 npm 发布。首次公开发布前需由维护者确定 LICENSE、复核素材权属、跑干净环境验证并审核部署的密钥与日志配置。发布时创建版本 tag，记录兼容性变化，尤其是签名链接与临时数据的失效条件。
