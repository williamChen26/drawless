{
  "name": "@drawless/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm run build && node dist/src/main.js",
    "smoke": "pnpm run build && node dist/src/scripts/smoke-sync-server.js",
    "smoke:coworker-control": "pnpm run build && node dist/src/scripts/smoke-coworker-control.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@drawless/shared": "workspace:*",
    "@fastify/websocket": "^11.0.2",
    "@tldraw/sync-core": "5.0.1",
    "fastify": "^5.3.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "ws": "^8.18.1"
  }
}
